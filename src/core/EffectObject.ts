import { Strip } from './Strip'
import { StripEffect } from './stripEffect'

export class EffectObject {
  update (
    strip: Strip,
    effect: StripEffect,
    time: number,
    isPlay: boolean,
    jump: boolean
  ) {
    // do nothing
  }
}
