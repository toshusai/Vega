import styled from "styled-components";

import { COLOR_BACKGROUND_NAME } from "@/app-ui/src";

export const Card = styled.div`
  display: flex;
  width: 100%;
  height: 100%;

  background-color: var(${COLOR_BACKGROUND_NAME});
`;
